version: "3"

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    ports:
      - "5432:5432"
    volumes:
      - postgres-db-volume:/var/lib/postgresql/data

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  airflow-webserver:
    image: apache/airflow:2.6.3
    depends_on:
      - postgres
      - redis
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
      - AIRFLOW__CORE__FERNET_KEY=qF6mYUl6qM08X1Z2p0F4iVhJb3oC7cV1zM0Xzq9wAtc=   # ← 生成したFernet Keyを設定
      - AIRFLOW__WEBSERVER__PORT=8080
    ports:
      - "8080:8080"
    volumes:
      - ./dags:/opt/airflow/dags
      - ./input:/opt/airflow/input
      - ./output:/opt/airflow/output
      - ./variables.json:/opt/airflow/variables.json
    command: webserver

  airflow-scheduler:
    image: apache/airflow:2.6.3
    depends_on:
      - airflow-webserver
      - postgres
      - redis
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
      - AIRFLOW__CORE__FERNET_KEY=qF6mYUl6qM08X1Z2p0F4iVhJb3oC7cV1zM0Xzq9wAtc=   # ← 同じキーを必ず設定
    volumes:
      - ./dags:/opt/airflow/dags
      - ./input:/opt/airflow/input
      - ./output:/opt/airflow/output
      - ./variables.json:/opt/airflow/variables.json
    command: scheduler

volumes:
  postgres-db-volume:
